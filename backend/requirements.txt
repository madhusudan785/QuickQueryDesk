# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Database
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
alembic==1.16.1
psycopg2-binary>=2.9.10  # sync driver, used only by Alembic (which needs a sync engine)

# Authentication
pyjwt==2.10.1
bcrypt==4.3.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.3
httpx==0.28.1

# Validation & Settings
pydantic==2.11.3
pydantic-settings==2.9.1
email-validator==2.3.0
python-dotenv==1.1.0

# LLM
groq>=0.18.0
google-generativeai==0.8.5
langchain-google-genai==2.0.7

# RAG / LangChain
langchain==0.3.25
langchain-community==0.3.24
langchain-huggingface==0.1.2
faiss-cpu==1.15.0
sentence-transformers==4.1.0

# CORS
python-multipart==0.0.20

# NOTE: WebSocket real-time updates are active in app/main.py (bundled via
# uvicorn[standard] above). The metrics and knowledge-base API routers are added
# in a later commit.
